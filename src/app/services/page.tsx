import Services from '@/components/Services';

export const metadata = {
  title: 'Services - Spedition Ahmed',
  description: 'Our services include local & long-distance transport, express delivery, machinery transport, and more.',
};

export default function ServicesPage() {
  return <Services />;
}